import 'package:flutter/material.dart';

class NotificationsComponent extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Notifications Component',
          style: TextStyle(color: Colors.black)),
    );
  }
}
