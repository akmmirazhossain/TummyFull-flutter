import 'package:flutter/material.dart';

class MyGroupComponent extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('My Group Component', style: TextStyle(color: Colors.black)),
    );
  }
}
