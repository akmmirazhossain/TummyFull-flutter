import 'package:flutter/material.dart';

class OrderPlaceComponent extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child:
          Text('OrderPlace Component', style: TextStyle(color: Colors.black)),
    );
  }
}
